# セッションログ: 2026-03-10-3 [JVA]

## 今日やったこと
- Google Calendar MCP 連携のセットアップ（Claude Code ↔ Google Calendar）
- OAuth2認証エラー（redirect_uri_mismatch）のデバッグ・解決
- `/company` スキルの検出ロジックバグを修正

## 完了タスク
- [x] Google Cloud Console で OAuth2 クライアントID・Secret 取得
- [x] `~/.config/google-calendar-mcp/credentials.json` 作成
- [x] `claude mcp add` で google-calendar を user スコープで登録
- [x] redirect_uri_mismatch エラー解決（`http://localhost:3500/oauth2callback` を GCCに追加）
- [x] OAuth認証完了 → `tokens.json` 生成確認
- [x] `/company` スキルの Step 1 検出を `.company*/` パターンに対応

## 未完了・持ち越し
- [ ] Notion MCP 連携（API token 取得 → `claude mcp add` 実行）
- [ ] Google Calendar MCP の動作テスト（実際にカレンダー操作）

## 更新したファイル
- `~/.config/google-calendar-mcp/credentials.json`（新規作成）
- `~/.claude.json`（google-calendar MCP 登録）
- `~/.claude/plugins/cache/cc-company/company/1.0.0/skills/company/SKILL.md`（検出ロジック修正）

## 重要な決定・発見
- Google Calendar MCP: `@cocal/google-calendar-mcp` パッケージを採用（`GOOGLE_OAUTH_CREDENTIALS` 環境変数でcredentials.jsonパスを指定）
- Notion MCP: 公式HTTP endpoint `https://mcp.notion.com/mcp` + `@notionhq/notion-mcp-server` が利用可能
- `/company` スキルは `.company/` しか検出しない仕様バグがあった → 修正済み

## 次回の推奨アクション（優先順）
1. Notion MCP 連携を完成させる（token 取得→登録→テスト）
2. Google Calendar MCP の動作確認（「今週の予定を教えて」等）
3. MCP連携を実際のJVA業務に活用（例：カレンダーにIBミーティング自動登録）
